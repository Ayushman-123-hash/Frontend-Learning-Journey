// ES6 Modules 
// It is used in both browser and Node.js environment. It uses import to import modules and export to export modules.
// We use this after ES6 modules were introduced. It is now the standard way to work with modules in JavaScript.

export const hello1 = () => {
    console.log("Hello Ayush");
}

export const ahello1 = (name) => {
    console.log("Hello" + name);
}

